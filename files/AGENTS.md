# Local delivery rule

For this project, write and deliver the current website files to:
`C:\Users\Narroi\Desktop\Projeto tomahawk SITE\v1.1\files`

Keep `files.zip` in that folder synchronized with the delivered HTML, CSS,
JavaScript, Markdown, and `assets` files. Do not delete unrelated files there.
